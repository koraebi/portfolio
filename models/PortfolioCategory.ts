class PortfolioCategory {
  id: string;
  picture: string;
  background: string;
  section: string;
 
  constructor(id: string, picture: string, background: string, section: string) {
    this.id = id;
    this.picture = picture;
    this.background = background;
    this.section = section;
  }
}